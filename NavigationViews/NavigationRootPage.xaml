<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WorkdayCalculator.NavigationViews.NavigationRootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WorkdayCalculator.NavigationViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <NavigationView x:Name="NavView"
                        MenuItemTemplateSelector="{StaticResource selector}"
                        MenuItemsSource="{x:Bind Cathegories, Mode=OneWay}"/>
        <local:MenuItemTemplateSelector x:Key="selector">
            <local:MenuItemTemplateSelector.ItemTemplate>
                <DataTemplate x:DataType="local:Cathegory">
                    <NavigationViewItem Content="{x:Bind Title}" ToolTipService.ToolTip="{x:Bind Tooltip}">
                        <NavigationViewItem.Icon>
                            <SymbolIcon Symbol="{x:Bind Glyph}" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </DataTemplate>
            </local:MenuItemTemplateSelector.ItemTemplate>
        </local:MenuItemTemplateSelector>
    </Grid>
</Page>
